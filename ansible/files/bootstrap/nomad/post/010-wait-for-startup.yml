- name: "{{ service }} - post - Wait for startup"
  uri:
    url: http://127.0.0.1:4646/v1/jobs
  register: result
  until: result.status == {% if lookup('env', 'nomad_acl') | bool %} 403 {% else %} 200 {% endif %}
  retries: 60
  delay: 1
