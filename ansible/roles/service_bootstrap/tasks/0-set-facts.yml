# Reset facts
- name: "{{ service }} - fact - pre_bootstrap_files"
  set_fact:
    pre_bootstrap_files: {}
- name: "{{ service }} - fact - post_bootstrap_files"
  set_fact:
    post_bootstrap_files: {}