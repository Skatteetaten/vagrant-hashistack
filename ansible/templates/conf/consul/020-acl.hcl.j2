{% if lookup('env', 'consul_acl') | bool -%}
acl {
  down_policy = "extend-cache"
  enabled = true
  tokens {
    master = "{{ lookup('env', 'consul_master_token') }}"
  }
  default_policy = "{{ lookup('env', 'consul_acl_default_policy') }}"
}
{% endif -%}